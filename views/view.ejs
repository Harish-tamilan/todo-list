<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        * {
            box-sizing: border-box;
        }
        
        body {
            font-family: Arial, Helvetica, sans-serif;
        }
        .to-do-form{
            align-items: center;
            text-align: center;
            color:black;
            border: 8px solid  grey;
            box-shadow: grey 10px 10px 10px;
            border-radius: 36px;
            padding: 8px 12px;
            background-color: rgba(28, 86, 133, 0.26);
            margin-bottom: 10px;
            
        }

        .column {
            float: left;
            width: 25%;
            padding: 0 10px;
        }
        
        .row {margin: 0 -5px;}
        
        .row:after {
            content: "";
            display: table;
            clear: both;
        }
        
        .card {
            marign-top: 10px;
            box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2); 
            padding: 16px;
            text-align: center;
            background-color: #f1f1f1;
        }
        .btn {
            display: inline-block;
            padding: 0.25rem 1rem;
            text-decoration: none;
            font: inherit;
            border: 1px solid #00695c;
            color: #00695c;
            background: white;
            border-radius: 3px;
            cursor: pointer;
        }
        @media screen and (max-width: 600px) {
            .column {
            width: 100%;
            display: block;
            margin-bottom: 20px;
            }
        }
        
        
    </style>
</head>
<body>
    <div class="to-do-form">
        <form class="item-form" action="<% if(edit) { %>/edit<% } else {%>/add <% } %>"  method="POST">
            <div class="form-control">
                <label for="title">TODO Action</label>
                <input type="text" name="title" id="title" value="<% if (edit) { %><%= item.title %><% } %>"/><br/><br/>
            </div>
            <label for="time">Time Limit</label>
            <input type="text" name="time" id="time" value="<% if (edit) { %><%= item.timeLimit %><% } %>"/><br/><br/>
            <% if (edit) { %>
                <input type="hidden" value="<%= item.id %>" name="id">
            <% } %>
            <button><% if (edit) { %>Edit<% } else { %>Add <% } %></button>
        </form>

    </div>
    <div class="row">
		<% for (let item of items ) { %>
			<div class="column">
			  <div class="card">
				<div class="content">
					<h1><center><%= item.title %></center></h1>
					  <h3><center>By <%= item.timeLimit %></center></h3>
					  <a href="/edit/<%= item.id %>" class="btn">Edit</a>
					  <a href="/delete/<%= item.id %>" class="btn">Delete</a>
			    </div>
			  </div>
            </div>
		<% } %>
    </div>
</body>
</html>